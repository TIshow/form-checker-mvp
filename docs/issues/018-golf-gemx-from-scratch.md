# ゴルフ × GEM-X を 0 から積み上げる（上流に手を入れない）

Status: Step 2 実施（2D への合わせ込み。画像面は合うが 3D の指標は改善せず。下の Step 2）
作成: 2026-09-21

## なぜリセットするか

015〜017 で、GEM-X の出力に対して再投影補正・色順パッチ・DDIM 切替・接地後処理の
比較・表示の足留めを重ねた。効いたものも効かなかったものも分かったが、
**どの数字がどの改変の結果なのか**が追いにくくなり、確認画面で見えた「飛び」が
表示側の処理だった（017 末尾）ように、改変そのものが新しい混乱を生み始めた。

だから一度すべて外して、**上流の GEM-X をそのまま走らせた出力**を土台に置き、
一段ずつ「何を足したら何が変わったか」を数字と画面で確かめながら積む。

015〜017 は記録として残す（結論はそのまま参照できる）。コードからは
`core/refinement.py`、`tools/refine_golf.py`、`backend/golf_capture.py`、
`backend/gemx_experiments.py`、`web/golf-review.html`、`reconstruct_gemx.py` の
色順・DDIM パッチを外した（2026-09-21）。出力ディレクトリも削除した。
017 までに分かったことのうち、次に積む候補になり得るもの:

| 分かったこと | 出典 |
|---|---|
| 上流デモは ViTPose に色を逆に渡している（2D の手首がトップで崩れる） | 016 |
| 上流の `--ddim` は効いていない。効かせると手・膝は揃うが速度ピークが 7% 落ちる | 017 |
| 残る最大の誤差は後ろ足の奥行き（+40cm）。2D は合っている。単眼の限界 | 017 末尾 |
| 表示の足留めは誤差を隠して一度に吐き出す | 017 末尾 |
| 再投影で 3D を補正すると画像には合うが 3D は壊れる | 015/016 |

## 進め方

各ステップで「入力・処理・出力・確認画面・数字」を一つずつ固定し、
前のステップとの差分だけを見る。**上流のコードは変えない**（変える必要が
出たら、それは別のステップとして明示する）。

### Step 1: 素の GEM-X（完了 2026-09-21）

- 入力: `videos/golf/golf_form1.mp4`（回転フラグを画素に焼くだけ。切り出しなし）
- 処理: `backend/reconstruct_gemx.py`（公式デモ `demo_soma.py -s` をそのまま。
  公式 SOMA アダプタで関節へ）
- 出力: `output_golf_gemx/`（`gx_joints.npy`、`gx_kp2d.npy`、公式のメッシュ描画、
  `provenance.json` に `upstream_modified: false`）
- 確認画面: `web/clip.html?clip=golf_gemx`（**表示の足留めなし**。`--no-anchor`）
- 数字: `python -m analysis --joints output_golf_gemx/gx_joints.npy --fps 30 --domain golf_swing`

ここで見えるものが「GEM-X の素の実力」。これ以降のステップは全部これとの差分。

**Step 1 の数字**（`upstream_modified: false`、動画 SHA `02b5d6b8…`、172 フレーム）:

| | 値 |
|---|---:|
| 局面 takeaway / top / impact / finish | 79 / 110 / 116 / 127 |
| 両手首の距離 トップ f108 / インパクト f117 / フィニッシュ f132-144 | 34 / 18 / 76〜78 cm |
| 両手首の距離の SD（スイング区間） | 7.3 cm |
| インパクトのリード膝 | 143° |
| 頭の上下動 | 7.2 cm |
| 錨の足の滑り（表示で留めていない生の値） | 0.58 m |

以前の同条件の記録（016 の「旧」列）と同じ水準。トップで両手が離れ、
フィニッシュで腕が崩れ、足は 58cm 滑る——これが土台。

### Step 2: SOMA の関節回転を 2D 検出に合わせる（実施 2026-09-21）

- 処理: `backend/gemx_fit2d.py`。変数は SOMA の関節回転の補正量（76 関節 × axis-angle）と
  root の画像面の並進。骨長・体型は Step 1 のまま、root の奥行きは固定（σ 5mm）。
  順運動学は上流の `SomaLayer`。損失 = 再投影（soft-L1、スコア 0.5 以上のみ）＋
  回転の事前分布（σ 0.15 rad）＋ 補正量の加速度（σ 0.02 rad）。Adam 400 反復、22 秒。
  5 フレームに 1 枚は合わせに使わず評価用に取り置く。上流のコードは不変。
- 出力: `output_golf_gemx_fit/`。確認画面: `web/clip.html?clip=golf_gemx_fit`
  （Step 2 と Step 1 のメッシュを並べる）
- 比較: `python tools/golf_step_report.py output_golf_gemx/gx_joints.npy output_golf_gemx_fit/gx_joints.npy`

| | Step 1 素 | Step 2 合わせ込み | GVHMR 参照 |
|---|---:|---:|---:|
| 再投影 中央値 合わせたフレーム / 取り置き | 26.7 / 26.5 px | **5.6 / 8.7 px** | — |
| 両手首 トップ f110 | 33 | 38 | 16 |
| 両手首 f108 / f124 | 34 / 25 | 46 / 57 | 16 / 13 |
| 両手首 インパクト | 23 | 21 | 16 |
| 両手首 フィニッシュ | 76〜78 | 64〜70 | 10〜16 |
| 両手首 SD スイング区間 | 7.3 | 9.6 | 1.2 |
| リード膝 インパクト | 143° | 139° | 149° |
| 両足首の奥行き差 トップ | 35 cm | 22 cm | 3 cm |
| 足の高さ差 最大 / 滑り | 17 cm / 0.58 m | 18 cm / 1.19 m | 7 cm / 0.08 m |
| 手の最高速度 | 5.7 m/s @115 | 5.7 @115 | 6.1 @119 |
| 全関節の最大移動 / 回転補正 最大 | — | 59.5 cm / 28° | — |

移動の内訳: 足首・つま先が平均 32cm（ほぼ奥行き、平均 28cm）、肩〜手首 8cm、
股・膝 8cm、骨盤・背骨 2.5cm。Step 1 の再投影誤差は脚（47〜92px）が腕（〜20px）
よりずっと大きく、合わせ込みは主に脚を動かした。

**分かったこと**

1. **画像面は合う。** 取り置きフレームでも 26→8.7px。メッシュはカメラ視点で
   体に乗る（脚・足はとくに）。過学習ではない。
2. **3D の指標は良くならない。** 両手首はトップで 33→38cm（f124 で 57cm）、
   膝 143→139°、滑りは倍。後ろ足の奥行き差だけ 35→22cm と縮んだ。
3. **理由は 015 と同じ構造。** 回転を変数にして骨長を守っても、四肢の奥行きは
   「カメラの光線に沿って回す」自由度として残る。root を固定しても、肩から先・股から
   先が奥行き方向に回れば 2D は同じに見える。**単眼の 2D は奥行きを決めない**——
   これが 2 回目の確認になった。K が未較正なのも効く（既定の焦点距離で
   奥行きと画像面の並進が混ざる）。
4. 手の 2D は指の点が多く（77 点中 44 点が手）、手首よりスコアが低い。腕の
   合わせ込みは手首・肘だけに絞る余地がある（未実施）。

**判断**: Step 2 は「2D に合わせる」ことはできるが、**それだけでは 3D は良くならない**。
採用しない。上流にも手を入れていないので、外すものは無い（コードは記録として残す）。
奥行きを決める情報を足さない限り、この方向の改良（点の選び方・重み）は
数 cm の話に留まる。

### Step 3 以降（未定）

奥行きを決める情報の候補: (a) 横撮り（測りたい量を画像面に入れる）、
(b) 2 台同期、(c) 床・クラブとの接触を目視で確認した区間だけ拘束。
上流を変える候補（色順・DDIM）は 016/017 のとおり画像面の手を直すが、
奥行きは直さない。

候補は上の表。順番は「上流を変えずにできること」→「入力を変えること
（撮り方）」→「上流を変えること」の順で、後ろほど慎重に。
